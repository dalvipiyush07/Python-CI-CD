pipeline {
    agent any

    environment {
        SSH_CRED = 'node-app-key'
        SERVER_IP = '3.110.168.120'
        REMOTE_USER = 'ubuntu'
        APP_DIR = '/home/ubuntu/pythonapp'
        PORT = '8888'
    }

    stages {
        stage('Clone Repository') {
            steps {
                git branch: 'main', 
                url: 'https://github.com/dalvipiyush07/Python-CI-CD.git',
                credentialsId: "${SSH_CRED}"
            }
        }

        stage('Verify Files') {
            steps {
                sh '''
                    echo "📁 Checking project files..."
                    ls -la
                    echo "✅ Files verified"
                '''
            }
        }

        stage('Deploy to Server') {
            steps {
                sshagent(credentials: ["${SSH_CRED}"]) {
                    sh """
                        echo "🚀 Deploying to ${SERVER_IP}"
                        ssh -o StrictHostKeyChecking=no ${REMOTE_USER}@${SERVER_IP} "mkdir -p ${APP_DIR}"
                        scp -o StrictHostKeyChecking=no app.py requirements.txt ${REMOTE_USER}@${SERVER_IP}:${APP_DIR}/
                        echo "✅ Files deployed"
                    """
                }
            }
        }

        stage('Setup Environment') {
            steps {
                sshagent(["${SSH_CRED}"]) {
                    sh """
                        ssh -o StrictHostKeyChecking=no ${REMOTE_USER}@${SERVER_IP} '
                            cd ${APP_DIR}
                            python3 -m venv venv
                            . venv/bin/activate
                            pip install -r requirements.txt
                            echo "✅ Environment ready"
                        '
                    """
                }
            }
        }

        stage('Stop Existing App') {
            steps {
                sshagent(["${SSH_CRED}"]) {
                    sh """
                        ssh -o StrictHostKeyChecking=no ${REMOTE_USER}@${SERVER_IP} '
                            pkill -f "python3 app.py" || true
                            sleep 2
                            echo "✅ Apps stopped"
                        '
                    """
                }
            }
        }

        stage('Start Application') {
            steps {
                sshagent(["${SSH_CRED}"]) {
                    sh """
                        ssh -o StrictHostKeyChecking=no ${REMOTE_USER}@${SERVER_IP} '
                            cd ${APP_DIR}
                            . venv/bin/activate
                            nohup python3 app.py > app.log 2>&1 &
                            echo "✅ App started"
                        '
                    """
                }
            }
        }

        stage('Verify Deployment') {
            steps {
                sshagent(["${SSH_CRED}"]) {
                    sh """
                        sleep 10
                        ssh -o StrictHostKeyChecking=no ${REMOTE_USER}@${SERVER_IP} '
                            if curl -s http://localhost:${PORT}/api/health > /dev/null; then
                                echo "✅ Health check passed"
                            else
                                echo "❌ Health check failed"
                                exit 1
                            fi
                        '
                    """
                }
            }
        }
    }

    post {
        success {
            echo "🎉 Success! App running at: http://${SERVER_IP}:${PORT}"
        }
        failure {
            echo "❌ Deployment failed"
        }
    }
}